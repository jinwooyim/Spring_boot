<!DOCTYPE mapper
    PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
    "https://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.boot.dao.MemberDAO">

    <insert id="registUser">
		insert into EV_user (user_id, user_password, user_name, user_email, user_province, user_city) 
		values
		(#{user_id}, #{user_password}, #{user_name}, #{user_email}, #{user_province}, #{user_city})
    </insert>
    
    <select id="user_id_check">
    	select count(*) from EV_user where user_id=#{user_id}
    </select>

    <select id="login">
    	select count(*) from EV_user where user_id=#{user_id} and user_password=#{user_password}
    </select>
</mapper>